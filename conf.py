# It is expected that the server will run on every host on the same port
PORT = 12344

# This is the IP address of the main host
MAINHOST = "192.168.1.90"

# This is the list of IP addresses of the different nodes to connect to
NODES = ["192.168.1.90", "192.168.1.64"]
